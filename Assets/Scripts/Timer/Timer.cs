using Mirror;
using TMPro;
using UnityEngine;

public class Timer : MonoBehaviour
{
    //[SerializeField] private TextMeshProUGUI _timeTxt;

    //public void TimeUpdate()
    //{
    //    _time += Time.deltaTime;
    //    _timeTxt.text = _time.ToString("F3");
    //}
}
